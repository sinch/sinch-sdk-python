# Direct dependencies. CI and local dev install from the compiled lock file
# (requirements-dev.txt). After editing this file, regenerate it with:
#   ./scripts/compile_requirements.sh

# Testing
pytest
pytest-mock
coverage
behave

# Code Quality
ruff == 0.15.22

# Tooling
uv

# HTTP Libraries
requests >= 2.32.5; python_version < "3.10"
requests >= 2.33.0; python_version >= "3.10"
urllib3 >= 2.6.3; python_version < "3.10"
urllib3 >= 2.7.0; python_version >= "3.10"

# Data Validation
pydantic >= 2.0.0

# Documentation
# Sphinx 7.1 introduced python_maximum_signature_line_length and  7.x is also the last series supporting Python 3.9.
sphinx >= 7.1
sphinx-rtd-theme >= 2.0
